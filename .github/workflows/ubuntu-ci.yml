name: Ubuntu-CI

'on':
  - push
  - pull_request

permissions:
  contents: read

jobs:
  ci:
    name: ubuntu-gcc-${{ matrix.config.name }}
    runs-on: ubuntu-latest

    strategy:
      fail-fast: false
      matrix:
        config:
          - { name: release, extra_flags: '' }
          - { name: debug,   extra_flags: '-DCMAKE_BUILD_TYPE=Debug' }

    env:
      CC: gcc
      CXX: g++

    steps:
      - uses: actions/checkout@8e8c483db84b4bee98b60c0593521ed34d9990e8 # v6.0.1

      - name: Configure build
        run: |
          cmake \
            -S . \
            -B build \
            -DCMAKE_INSTALL_PREFIX="${GITHUB_WORKSPACE}/install" \
            -DENABLE_ROARING_TESTS=ON \
            -DROARING_UNSAFE_FROZEN_TESTS=ON \
            ${{ matrix.config.extra_flags }}

      - name: Build/test/install
        run: |
          cmake --build build
          ctest --test-dir build --output-on-failure
          cmake --install build

      - name: Build installation-find sample
        run: |
          cmake \
            -S tests/installation/find \
            -B tests/installation/find/build \
            -DCMAKE_PREFIX_PATH="${GITHUB_WORKSPACE}/install" \
            ${{ matrix.config.extra_flags }}
          cmake --build tests/installation/find/build
